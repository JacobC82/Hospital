spring:
  datasource:
    username: hospital_application
    password: hospital_application
    url: jdbc:mysql://localhost:3306/hospital_application
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true
  
  sql:
    init:
      mode: always
